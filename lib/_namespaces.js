/* global Models: true, Collections: true, Utils: true */

// This file exists so that we can do "use strict" in all other files and
// still have some global namespace variables.

// It is called what it's called and palce where it's placed so that it loads
// as early as possible.

Models = {};
Collections = {};
